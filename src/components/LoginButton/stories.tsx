import { Story, Meta } from '@storybook/react/types-6-0'
import LoginButton from '.'

export default {
  title: 'LoginButton',
  component: LoginButton
} as Meta

export const Default: Story = () => <LoginButton />
